// Re-export shared routing constants for backward compatibility
export {
  STATE_CALCULATING_INIT,
  STATE_CALCULATING_LOADED,
  STATE_CALCULATING_LOADING,
  STATE_CALCULATING_RELOADING,
  STEP_KIND_DEPART,
  STEP_KIND_NORMAL,
  STEP_KIND_ARRIVE,
  STEP_KIND_BOTH,
} from "~/utils/routing/constants";
