name: probe-negated-path

on:
  push:
    branches:
      - main
    paths:
      - '!app/**'

jobs:
  fired:
    runs-on: docker
    steps:
      - run: |
          echo "probe-negated-path fired"
          echo "ref=${{ github.ref }}"
          echo "sha=${{ github.sha }}"
          echo "before=${{ github.event.before }}"